server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/AgustinHaristoy/Lab2-Tingeso
          search-paths: config-data
  application:
    name: config-service